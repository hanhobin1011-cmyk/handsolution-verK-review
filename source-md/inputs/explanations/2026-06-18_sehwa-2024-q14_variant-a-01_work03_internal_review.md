# 세화 2024 Q14 변형 A-01 작업03 내부 검수 보고서

```yaml
schemaVersion: handsolution-review-report-v1
runId: sehwa-q14-variant-a-work03-operator
problemCode: sehwa-2024-q14-variant-a-01
workNo: 03
status: pass_for_review_candidate
```

## 검수 결론

작업03 page1/page2는 검수 후보로 등록 가능하다.

## 작업02 피드백 반영

### page1

- 제목의 `손풀이` 오탈자를 확인했다.
- 흰 배경과 검정 기본 글씨를 유지했다.
- 밑줄만 있는 형태가 아니라 조건 변환, 핵심 치환, Tip, 보조 그림을 포함해 손풀이 흐름을 보강했다.
- 보기 ①~⑤가 모두 보인다.

### page2

- 그래프 안 복잡한 분수 좌표 대신 `r=p/√2`를 먼저 정의하고 `B(p-r,0)`, `D(p+r,0)`로 표기했다.
- 그래프 라벨을 점과 선에서 떨어뜨려 배치했다.
- A(0,-4)는 y축 위에 있고, E(2p,-4)는 오른쪽 아래 점으로 보인다.
- 계산식 설명을 `r` 소거, `5p=25`, `p=5`, `f(5)=9` 순서로 보강했다.
- 정답은 `③ 9`로 표시했다.

## 수학 검수

- `g(x)=f(x)-x`
- 가능한 중심 x좌표는 `g(x)=-4,0,4`와의 교점으로 정리된다.
- `x1=0`, `f(x1)<0`에서 `g(0)=-4`.
- `g(x)=4-a(x-p)^2`, `4-ap²=-4`, `ap²=8`.
- `r=p/√2`이면 좌표는 `0, p-r, p, p+r, 2p`.
- `x2+x3+x4+x5=(p-r)+p+(p+r)+2p=5p=25`.
- `p=5`, `f(5)=g(5)+5=4+5=9`.

검산 통과.

## 남은 검수 포인트

page1 하단의 조건 변환 세부 유도는 학생 이해 보조용으로 들어갔으므로, 호빈님 검수에서 과밀하거나 불필요하다고 판단되면 다음 작업에서 더 간결하게 줄인다.
