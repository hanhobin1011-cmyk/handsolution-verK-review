# 세화 2024 Q14 변형 A-01 작업02 내부 검수 보고서

```yaml
schemaVersion: handsolution-review-report-v1
runId: sehwa-q14-variant-a-work02-operator
problemCode: sehwa-2024-q14-variant-a-01
workNo: 02
status: pass_for_review_candidate
```

## 검수 결론

작업02 page1/page2를 검수 후보로 등록 가능하다고 판단한다.

## 작업01 피드백 반영

- page1 배경을 흰 종이 톤으로 유지했다.
- page1 기본 글씨를 검정 중심으로 유지했다.
- 제목의 `손풀이` 오탈자를 확인했다.
- page2 그래프 영역은 데이터 기반 하네스로 재합성했다.
- page2 그래프의 A점은 실제 y축 위 `A(0,-4)`에 배치했다.
- p 도출 전 좌표는 `p` 기준으로 표시했다.
- 정답은 `③ 9`로 표시했다.

## 수학 검수

- `g(x)=f(x)-x`
- `g(0)=-4`
- `g(x)=4-a(x-p)^2`, `ap²=8`
- `x1=0`, `x2=p-p/√2`, `x3=p`, `x4=p+p/√2`, `x5=2p`
- `x2+x3+x4+x5=5p=25`
- `p=5`
- `f(5)=g(5)+5=4+5=9`

검산 통과.

## 시각 검수

- page1 원문과 보기 ①~⑤가 보인다.
- page2 그래프는 아래로 열린 포물선이며 `y=4`, `y=0`, `y=-4` 기준선이 보인다.
- 그래프 라벨은 repo-local `kyunghoonmath` 기반 래스터 라벨로 생성했다.
- `√` glyph는 해당 폰트에서 직접 안정 표시되지 않아 그래프 라벨에서는 `r2` 약식 표기로 두고, 바로 위 풀이 본문에는 세로형 `p/√2`가 표시되어 있다.

## 남은 검수 포인트

호빈님 검수에서 그래프 라벨의 `r2` 약식 표기를 더 엄격히 보려면 다음 작업에서 루트 기호를 별도 획 하네스로 그리는 방식으로 개선한다.
