# attempt14 page2 사용자 수정 피드백

대상:

```text
outputs/2026-06-18_sehwa-2024-q14_상세형_pipeline-test-01_attempt14_page2.png
```

판정:

```text
수정 필요
```

메모:

```text
수정지시에 이미지를 붙여서 설명하는 것도 필요해보인다. 이미지 Ctrl+V해서 붙여넣을 수 있게 피드백 칸 수정 필요.
이차함수인데 왼쪽이 휘어져서 들어가 있음.
```

첨부 이미지:

```text
work/review-feedback/attempt14-page2/user-feedback-graph-left-curve.jpg
```

attempt15 hard gate:

```text
- g(x)=4-a(x-p)^2는 이차함수 포물선이므로 전체 곡선이 매끈한 단일 포물선이어야 한다.
- 왼쪽 가지 A(0,-4) → B → C(p,4)는 안쪽으로 S자처럼 휘어 들어가면 실패다.
- 왼쪽 가지와 오른쪽 가지는 C(p,4)를 축으로 대칭적인 포물선 느낌을 가져야 한다.
- A(0,-4)는 실제 y축/x=0과 y=-4의 교점이어야 한다.
- 2p는 오른쪽 E(2p,-4)에만 붙인다.
```
