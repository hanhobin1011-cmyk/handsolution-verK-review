# 마전고 26년 고2 6월 Q07 작업05 visual spec

```yaml
schemaVersion: handsolution-visual-spec-v1
sourceType: original-graph-regeneration
visualKind: graph
problemCode: majeongo-g2-june-q07
candidate: outputs/2026-06-19_majeongo-g2-june_q07_상세형_작업05.png
harnessRef: scripts/render_majeongo_g2_june_q07_work05_visual_harness.py
```

## 1. 원본/목적

원본 그래프/도형 존재 여부: 절댓값 탄젠트 그래프와 점 O, A, B, C, P 및 두 음영 삼각형이 원문에 포함된다.

그림 목적: `x=2π` 전후로 함수식이 달라지는 구조와 직선 `A-B-C`, 두 삼각형 `AOB`, `BCP`의 넓이비 `7:3`을 한눈에 보이게 한다.

학생이 이 그림에서 읽어야 할 관계: `A(a,4u)`, `C(4π-a,-2u)`, `B(b,0)`, `P(4π,0)`, `b=28π/13`, `a+b=48π/13`.

## 2. 좌표계/구성 데이터

좌표계 정의: x축은 `0`부터 `4π`까지, 중간에 `x=2π` 점근선/구분선을 세운다. y축은 A와 C가 모두 박스 안에 들어오도록 자동 패딩을 둔다.

필수 점: `O=(0,0)`, `A=(20π/13,4u)`, `B=(28π/13,0)`, `C=(32π/13,-2u)`, `P=(4π,0)`.

필수 선/곡선/호: `0<x<2π` 구간의 `4tan(x/4)` 가지, `2π<x<4π` 구간의 `2tan(x/4)` 가지, 직선 `A-B-C`, x축, y축, `x=2π` 기준선.

필수 음영/색칠 경계: `△AOB`는 O-A-B-O 경계의 연한 파랑, `△BCP`는 B-C-P-B 경계의 연한 초록. 축/곡선/점/라벨을 가리지 않는 투명도만 사용한다.

필수 라벨: `O`, `A`, `B`, `C`, `P`, `x=2π`, `2π`, `4π`, `28π/13` 세로분수.

라벨 표기 규칙: 그래프/도형 라벨은 손풀이 통합용 래스터 라벨로 배치한다. `π`는 실제 파이 글리프를 쓰고, `28π/13`은 세로분수로 쓴다. ASCII pi 표기는 금지한다.

금지 라벨/표현: `2pi`, `4pi`, `28pi/13`, 슬래시식 `28π/13`, 그래프 잘림, 진한 음영으로 곡선/점 가림.

## 3. 박스/배치 안전 규칙

그래프/도형 박스 위치: raw 손글씨 베이스의 문제 블록 아래, 풀이 시작 위 안전 박스 `(90,420,934,715)`에 합성한다.

비그래프 손글씨 영역 침범 금지 기준: 문제 본문, 선지, 풀이 줄, 정답 박스, Tip 글자는 하네스가 새로 렌더링하지 않는다. 그래프 박스는 풀이 첫 줄을 침범하지 않아야 하며, 하단 여백을 남긴다.

## 4. 하네스 assert

- 모든 필수 점이 그래프 박스 내부에 있어야 한다.
- `B`는 `2π` 오른쪽에 있어야 한다.
- `28π/13`은 세로분수로 표시한다.
- `π`는 실제 파이 글리프로 표시하고 tofu 박스가 없어야 한다.
- `△AOB`, `△BCP` 음영은 연하게 표시되어 선과 점을 가리지 않아야 한다.

## 5. Review Agent 체크

- 그래프가 잘리지 않는지 확인한다.
- `π` 라벨이 ASCII pi나 tofu 박스로 보이지 않는지 확인한다.
- 분수 라벨이 세로 형태인지 확인한다.
- 문제/풀이/정답/Tip 비그래프 글자에는 코드 렌더링이 새로 들어가지 않았는지 확인한다.
