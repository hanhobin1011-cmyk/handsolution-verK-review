# Q07 작업08 Visual Spec

schemaVersion: handsolution-visual-spec-v1
sourceType: user-feedback-regeneration
visualKind: graph
원본 그래프/도형 존재 여부: 원문 함수 그래프가 핵심 조건이며, 풀이용 좌표 그래프를 데이터 기반으로 재작성한다.
그림 목적: `A`, `B`, `C`, `P` 위치와 `△AOB:△BCP=7:3` 넓이비를 축 위에서 바로 읽게 한다.
학생이 이 그림에서 읽어야 할 관계: `B`가 `2π` 오른쪽에 있고, `△AOB`와 `△BCP`가 같은 높이 `u` 단위로 넓이비를 만든다.
교재형 시각 설계: 함수 그래프와 좌표축을 먼저 보이게 하고, 넓이 삼각형은 연한 빗금으로만 보조 표시한다.
원 함수 그래프 중 반드시 보존할 부분: `0≤x<2π`의 파란 탄젠트 가지, `2π<x≤4π`의 초록 탄젠트 가지, 점 `A`, `B`, `C`, `P`.
도형/보조선을 얹는 순서: 좌표축 → `x=2π` 보조선 → 빗금 삼각형 → 원 함수 곡선 → 직선 `AC`와 점 라벨.
학생 시선 이동 순서: 원점 `O` → 파란 가지의 `A` → x절편 `B` → 초록 가지의 `C` → `P(4π,0)` → 두 빗금 삼각형 넓이비.

## 1. 원본/목적

- source: 마전고 26년 고2 6월 손풀이 7번
- baseRaw: `work/generated_raw/2026-06-19_majeongo-g2-june_q07_work07_raw.png`
- output: `outputs/2026-06-19_majeongo-g2-june_q07_상세형_작업08.png`
- harness: `scripts/render_majeongo_g2_june_q07_work08_visual_harness.py`
- 사용자 반려 하드 게이트: 좌표축이 사라져 보이면 실패, 삼각형을 면 채움으로 처리하면 실패.

## 2. 좌표계/구성 데이터

좌표계 정의: 그래프 안전 박스 `(88,442,770,872)` 안에서 `x∈[0,4π]`, `y∈[-7,12]`를 선형 좌표로 변환한다.
필수 점: `O=(0,0)`, `P=(4π,0)`, `a=20π/13`, `A=(a,4tan(a/4))`, `b=28π/13`, `B=(b,0)`, `C=(4π-a,-2tan(a/4))`.
필수 선/곡선/호: x축, y축, `x=2π` 세로 보조선, `0≤x<2π` 파란 함수 조각, `2π<x≤4π` 초록 함수 조각, 직선 `AC`.
필수 음영/색칠 경계: `△AOB`와 `△BCP` 내부를 연한 대각선 빗금으로만 표시한다. 불투명 면 채움은 금지한다.
필수 라벨: `O`, `P`, `A`, `B`, `C`, `x`, `y`, `0`, `2π`, `4π`, `x=2π`, `b=28π/13` 세로분수.
라벨 표기 규칙: `π`와 분수 라벨은 kyunghoonmath 기반 래스터 레이어를 사용한다. 그래프 라벨만 코드 렌더링 허용 범위다.
금지 라벨/표현: ASCII `2pi`, `4pi`, `pi/13` 식, 불투명 색칠 삼각형, 축 없는 넓이 그림.

## 3. 박스/배치 안전 규칙

그래프/도형 박스 위치: `(88,442,770,872)`.
비그래프 손글씨 영역 침범 금지 기준: 문제 본문, 선지, 풀이, 정답, 호빈T의 팁은 raw 손글씨형 베이스를 유지하며 하네스가 수정하지 않는다.

## 4. 하네스 assert

- `B[0] > 2π` 확인.
- 파란/초록 곡선 샘플 점 수가 충분한지 확인.
- `O/P/A/B/C` 점이 그래프 박스 내부 또는 축 화살표 허용 범위에 있는지 확인.
- 렌더 후 `scripts/validate_harness_font_policy.py scripts/render_majeongo_g2_june_q07_work08_visual_harness.py` 통과.

## 5. Review Agent 체크

- 좌표축과 화살표가 최종 PNG 단독으로 보이는지 확인한다.
- 두 삼각형이 연한 빗금이며 면 채움으로 보이지 않는지 확인한다.
- 원 함수 곡선이 빗금 위에 다시 그려져 가려지지 않는지 확인한다.
- `b=28π/13`, `2π`, `4π` 라벨이 겹치지 않는지 확인한다.
