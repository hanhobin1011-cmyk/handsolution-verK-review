# Visual Spec: 마전고 Q11 작업04

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-g2-june-full-batch-20260618161108
problemCode: majeongo-g2-june-q11
workId: 작업04
sourceType: coordinate-rendered
visualKind: graph
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: no
- 원본 참고 이미지 경로: `none`
- 그림 목적: 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. 왼쪽 사인 조각은 `0≤x<a`에서 1.5회 진동한다.
  2. 오른쪽 포물선은 `x=4`에서 전체 최솟값 `g(4)=-1`을 갖는다.
  3. `h(1)`부터 `h(5)`는 수평선 `y=g(n)`과 그래프의 교점 개수로 이해한다.

## 2. 좌표계/구성 데이터

- 좌표계 정의: `x=[0,5.4]`, `y=[-1.3,1.3]`, `a=18/5`.
- 축 범위/스케일: 작은 보조 그래프. 전체 페이지의 주인공이 아니라 h 값 설명 도구.
- 필수 점: `a=18/5`, `(4,-1)`, `(3,1)`.
- 필수 선/곡선/호: `y=sin(5πx/6)` for `0≤x<18/5`, `y=25/4(x-4)^2-1` for `x≥18/5`.
- 필수 음영/색칠 경계: 없음. 필요 시 h 수평선은 연한 점선으로만 표시.
- 필수 라벨: `a=18/5`는 세로분수, `(4,-1)`, `g(3)=1`, `h(1)`, `h(2)`, `h(3)`, `h(4)`, `h(5)`.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - `18/5`는 세로분수 형태로 표기한다.
  - 함수값/개수 라벨은 반드시 `h(1)`, `h(2)`, `h(3)`, `h(4)`, `h(5)`로 쓴다.
- 금지 라벨/표현:
  - `pi`, `h1`, `h2`, `h3`, `h4`, `h5`, `18/5` 단순 가로식, 진한 불투명 음영
  - 그래프가 해설 본문을 가리는 큰 박스

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: 문제 원문 아래 또는 풀이 옆의 작은 보조 박스. 해설 본문 위를 덮지 않음.
- 최대 박스 높이: 전체 높이의 23% 이하.
- 비그래프 손글씨 영역 침범 금지 기준: 그래프 박스 아래쪽은 풀이 첫 줄과 최소 35px 이상 떨어진다.
- 라벨 잘림 방지 기준: 세로분수 `18/5`, `h(n)` 라벨은 박스 오른쪽/아래 경계와 20px 이상 간격.
- 그래프가 설명을 돕는 방식: h 값 표가 단순 표가 아니라 수평선/교점 개수와 연결되어야 한다.

## 4. 하네스 assert

- [ ] 모든 필수 점이 박스 안에 있다.
- [ ] 모든 필수 라벨이 잘리지 않는다.
- [ ] 사인 조각은 1.5 cycles를 보인다.
- [ ] 포물선 꼭짓점은 `(4,-1)`이다.
- [ ] `a=18/5`는 세로분수형 라벨 계획을 가진다.
- [ ] `h(1)`부터 `h(5)`까지 괄호 표기다.
- [ ] 음영은 지정된 영역만 칠한다.
- [ ] 축/곡선/기준선이 음영에 가려지지 않는다.
- [ ] 금지 라벨 문자열이 없다.
- [ ] 그래프/도형이 해설 본문을 덮지 않는다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: 원본 없음, 좌표렌더링 풀이 보조 그래프.
- 수학 불변량 검증: `a=18/5`, `c=25/4`, `g(10)=224`.
- 교육과정 표기 검증: `h(n)` 괄호 표기, 세로분수, `π` 표기.
- 설명 도구로서의 적합성: h 값이 그래프 위치/수평선과 연결되어야 함.
- 최종 판정: visual gate 통과 전 등록 금지.
