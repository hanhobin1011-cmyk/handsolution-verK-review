# Visual Spec: 마전고 Q07 작업05

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-g2-june-full-batch-20260618161108
problemCode: majeongo-g2-june-q07
workId: 작업05
sourceType: coordinate-rendered
visualKind: graph
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: no
- 원본 참고 이미지 경로: `none`
- 그림 목적: 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. `x=2π` 점근선을 기준으로 왼쪽/오른쪽 조각이 다르다.
  2. `A-B-C` 직선의 x절편 `B`가 `x=2π` 오른쪽에 있다.
  3. 삼각형 `AOB`와 `BCP`의 넓이비가 해설식과 연결된다.

## 2. 좌표계/구성 데이터

- 좌표계 정의: x축 `0`부터 `4π`, y축은 `A(a,4u)`, `C(4π-a,-2u)`가 모두 보이도록 자동 여백 포함.
- 축 범위/스케일: `x=[0,4π]`, `y`는 필수점 기준 상하 여백 15%.
- 필수 점: `O`, `P(4π,0)`, `A(a,4u)`, `B(28π/13,0)`, `C(4π-a,-2u)`, `a=20π/13`.
- 필수 선/곡선/호: `x=2π` 점근선, 왼쪽 `4tan(x/4)`, 오른쪽 `2tan(x/4)`, 직선 `A-B-C`.
- 필수 음영/색칠 경계: `△AOB`, `△BCP`는 연한 색/빗금으로 구분하되 그래프를 가리지 않음.
- 필수 라벨: `O`, `P`, `A`, `B`, `C`, `x=2π`, `b=28π/13`.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - `28π/13`, `48π/13` 같은 분수는 그래프 라벨에서는 세로분수 또는 충분히 읽히는 수식형으로 표기한다.
  - 함수값/개수 라벨은 `h(1)`처럼 교육과정 표기로 쓴다.
- 금지 라벨/표현:
  - `pi`, `2pi`, `4pi`, `28pi/13`, `h1`, `h2`, `18/5` 단순 가로식, 진한 불투명 음영
  - A나 C가 박스 밖으로 잘리는 그래프

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: 문제 원문 아래, 풀이 첫 줄 위. 하단은 풀이와 최소 30px 간격.
- 최대 박스 높이: 전체 높이의 25% 이하.
- 비그래프 손글씨 영역 침범 금지 기준: 그래프 배경이 손글씨 풀이를 덮으면 실패.
- 라벨 잘림 방지 기준: A/B/C/P/O와 `x=2π` 라벨은 박스 안쪽 20px 이상.
- 그래프가 설명을 돕는 방식: 넓이비 계산에 필요한 두 삼각형과 B 위치가 명확해야 한다.

## 4. 하네스 assert

- [ ] 모든 필수 점이 박스 안에 있다.
- [ ] 모든 필수 라벨이 잘리지 않는다.
- [ ] `B_x > 2π`.
- [ ] A와 C가 박스 안에 있고 직선 A-B-C가 보인다.
- [ ] 음영은 지정된 영역만 칠한다.
- [ ] 축/곡선/기준선이 음영에 가려지지 않는다.
- [ ] 금지 라벨 문자열이 없다.
- [ ] 그래프/도형이 해설 본문을 덮지 않는다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: 원본 없음, 좌표렌더링 풀이 보조 그래프.
- 수학 불변량 검증: `a=20π/13`, `b=28π/13`, `a+b=48π/13`.
- 교육과정 표기 검증: 파이는 `π`, 분수는 수식형/세로형, ASCII 라벨 금지.
- 설명 도구로서의 적합성: 넓이비의 두 삼각형과 점근선 기준이 보여야 함.
- 최종 판정: visual gate 통과 전 등록 금지.
