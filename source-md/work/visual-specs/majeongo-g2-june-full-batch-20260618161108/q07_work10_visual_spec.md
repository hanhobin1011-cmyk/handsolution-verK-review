# Visual Spec: 마전고 Q07 작업10

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-g2-june-full-batch-20260618161108
problemCode: majeongo-g2-june-q07
workId: 작업10
sourceType: coordinate-rendered
visualKind: graph
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: no
- 원본 참고 이미지 경로: none
- 그림 목적: 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. 원 함수 그래프와 축이 먼저 보인다.
  2. 보조점/보조선/음영은 해설을 돕는 레이어다.
  3. 최종 산출물에 그래프 박스/테두리가 보이지 않는다.
- 교재형 시각 설계:
  - 원 함수 그래프 중 반드시 보존할 부분: 전체 곡선과 축.
  - 도형/보조선을 얹는 순서: 원 함수/축 → 보조점/교점 → 보조선/음영 → 라벨.
  - 강조할 부분과 약하게 둘 부분: 함수/축/교점 강조, 음영은 약하게.
  - 학생 시선 이동 순서: 원 함수 → 핵심점/교점 → 조건식 → 정답.

## 2. 좌표계/구성 데이터

- 좌표계 정의: 하네스 내부 좌표계로 문제 함수식을 직접 렌더링한다.
- 축 범위/스케일: 하네스 스크립트의 xmin/xmax/ymin/ymax 고정.
- 필수 점: Q07: tan branches, O/P/A/B/C, axes visible, P/4π separated, triangles hatched not filled, no graph box
- 필수 선/곡선/호: 원 함수 그래프, x축, y축, 필요한 보조선.
- 필수 음영/색칠 경계: 지정된 도형/교점 영역 내부만.
- 필수 라벨: 함수식, 핵심점, 필요한 축 눈금/기준선.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - 분수는 가능한 세로분수 형태로 표기한다.
  - 함수값/개수 라벨은 `h(1)`처럼 교육과정 표기로 쓴다.
  - 수식/분수/그래프 라벨은 `scripts.harness.labels`의 kyunghoonmath 렌더링 유틸을 사용한다.
- 금지 라벨/표현:
  - ASCII p-i, h-one/h-two, 가로식 분수, 진한 불투명 음영, visible graph box

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: 하네스 스크립트 box tuple. 최종 출력에는 테두리를 남기지 않는다.
- 최대 박스 높이: 손글씨 풀이를 침범하지 않는 범위.
- 비그래프 손글씨 영역 침범 금지 기준: 문제/풀이/정답/호빈T의 팁을 덮지 않는다.
- 라벨 잘림 방지 기준: 모든 라벨은 그래프 영역 안쪽 여백에 둔다.
- 그래프가 설명을 돕는 방식: 한국 고등학교 교재형 그래프처럼 원 함수와 축을 먼저 보여준다.

## 4. 하네스 assert

- [x] 모든 필수 점이 박스 안에 있다.
- [x] 모든 필수 라벨이 잘리지 않는다.
- [x] 원/호/곡선 위 점은 거리/방정식 검증을 통과한다.
- [x] 음영은 지정된 영역만 칠한다.
- [x] 축/곡선/기준선이 음영에 가려지지 않는다.
- [x] 금지 라벨 문자열이 없다.
- [x] 수식/분수 라벨이 kyunghoonmath 유틸로 렌더링되었다.
- [x] 원 함수 그래프가 도형/음영 때문에 잘려 보이지 않는다.
- [x] 교재형 해설 순서가 보인다: 원 함수 → 기준점 → 보조선/도형 → 음영/결론.
- [x] 그래프/도형이 해설 본문을 덮지 않는다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: pass
- 수학 불변량 검증: pass
- 교육과정 표기 검증: pass
- 설명 도구로서의 적합성: pass
- 최종 판정: pass


## 최신 DB 피드백 추가 게이트
- 풀이 시작 전에 `풀이)` 구분 표시를 둔다.
- 그래프/수식/라벨 겹침을 별도 확인한다.
