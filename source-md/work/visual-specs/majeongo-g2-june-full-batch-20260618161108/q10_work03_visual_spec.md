# Visual Spec: 마전고 Q10 작업03

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-g2-june-full-batch-20260618161108
problemCode: majeongo-g2-june-q10
workId: 작업03
sourceType: original-reference-plus-coordinate-rendered
visualKind: figure
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: yes
- 원본 참고 이미지 경로: `work/pdf-analysis/majeongo-g2-june/pages/page_05.png`
- 그림 목적: 문제 조건 보존 + 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. `BC`가 지름인 반원 위에 `D`, `E`가 있다.
  2. `D`는 `AC`와 반원의 교점이고, `E`는 작은 호 `CD` 위 점이다.
  3. 음영은 삼각형 `DCE` 하나이며, `CE=k`를 구한다.

## 2. 좌표계/구성 데이터

- 좌표계 정의: 원본 형태 좌표 `B=(0,0)`, `C=(5,0)`, `A=(2,4)`, 반원 중심 `O=(2.5,0)`, 반지름 `2.5`.
- 축 범위/스케일: 축은 표시하지 않는다. 도형은 원본처럼 `B` 왼쪽 아래, `C` 오른쪽 아래, `A` 위쪽에 배치한다.
- 필수 점: `A`, `B`, `C`, `D=(3.2,2.4)`, `E`는 원 `distance(E,O)=2.5`와 작은 호 `CD` 위 조건을 만족하는 점.
- 필수 선/곡선/호: 삼각형 `ABC`, 선분 `AC`, 지름 `BC`, 위쪽 반원 `BC`, 선분 `DE`, `EC`, `DC`.
- 필수 음영/색칠 경계: 삼각형 `DCE`만 연한 파란색 또는 빗금으로 표시.
- 필수 라벨: `A`, `B`, `C`, `D`, `E`, `BC=5`, `CD=3`, `CE=k`.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - 분수는 가능한 세로분수 형태로 표기한다.
  - 함수값/개수 라벨은 `h(1)`처럼 교육과정 표기로 쓴다.
  - Q10 도형 내부에는 ASCII 파이/분수 라벨을 쓰지 않는다.
- 금지 라벨/표현:
  - `pi`, `h1`, `h2`, `18/5` 단순 가로식, 진한 불투명 음영
  - `E`가 현 `CD` 위 점처럼 보이는 배치
  - `E`가 반원 밖/안쪽 임의점처럼 보이는 배치

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: 페이지 중앙 상단, 문제 원문 아래, 풀이 첫 줄 위. 비그래프 손글씨를 덮지 않는 독립 박스.
- 최대 박스 높이: 전체 높이의 28% 이하.
- 비그래프 손글씨 영역 침범 금지 기준: 도형 박스 아래쪽은 풀이 첫 줄과 최소 30px 이상 떨어진다.
- 라벨 잘림 방지 기준: 모든 라벨은 박스 경계에서 20px 이상 안쪽.
- 그래프가 설명을 돕는 방식: 원주각 90도, `CD=3`, `CE=k`, 음영 `DCE`가 한눈에 보여야 한다.

## 4. 하네스 assert

- [ ] 모든 필수 점이 박스 안에 있다.
- [ ] 모든 필수 라벨이 잘리지 않는다.
- [ ] 원/호/곡선 위 점은 거리/방정식 검증을 통과한다.
- [ ] `abs(distance(E,O)-2.5) < tolerance`.
- [ ] `E`는 작은 호 `CD` 위에 있다.
- [ ] `D`는 `AC`와 반원 위에 있다.
- [ ] 음영은 지정된 영역만 칠한다.
- [ ] 축/곡선/기준선이 음영에 가려지지 않는다.
- [ ] 금지 라벨 문자열이 없다.
- [ ] 그래프/도형이 해설 본문을 덮지 않는다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: 원본 도형 형태 보존 + 좌표 검산.
- 수학 불변량 검증: `AB=2√5`, `AC=BC=5`, `CD=3`, `CE²=4/5`, `60k²=48`.
- 교육과정 표기 검증: 점/선분 라벨만 사용, 코드식 라벨 금지.
- 설명 도구로서의 적합성: E가 호 위에 있고 음영 DCE가 풀이 목적을 설명해야 함.
- 최종 판정: visual gate 통과 전 등록 금지.
