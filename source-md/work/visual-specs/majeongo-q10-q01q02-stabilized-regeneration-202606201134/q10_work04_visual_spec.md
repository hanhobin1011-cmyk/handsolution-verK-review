# Visual Spec: 마전고 Q10 작업04 전체 재생성

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-q10-q01q02-stabilized-regeneration-202606201134
problemCode: majeongo-g2-june-q10
workId: 작업04
sourceType: original-reference-plus-coordinate-rendered
visualKind: figure
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: yes
- 원본 참고 이미지 경로: `work/pdf-analysis/majeongo-g2-june/pages/page_05.png`
- 그림 목적: 문제 조건 보존 + 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. `BC`가 지름인 반원 위에 `D`, `E`가 있고, 지름을 보는 원주각으로 직각 구조가 생긴다.
  2. `A-D-C`는 한 직선이며, `D`는 `AC`와 반원의 교점이다.
  3. `E`는 작은 호 `CD` 위에서 `C`에 가까운 점이고, 음영은 `△DCE` 하나만 나타낸다.
- 교재형 시각 설계:
  - 원 함수 그래프 중 반드시 보존할 부분: 함수 그래프가 아니라 원문 도형이다. 큰 삼각형 `ABC`, 지름 `BC`, 위쪽 반원, `A-D-C` 직선, 호 `CD`를 보존한다.
  - 도형/보조선을 얹는 순서: 큰 삼각형 `ABC` → 반원 `BC` → 점 `D`, `E` → 선분 `DE`, `EC`, `DC` → 옅은 `△DCE` 음영 → 길이/점 라벨.
  - 강조할 부분과 약하게 둘 부분: `△DCE`, `CE=k`, `CD=3`은 강조하고, 반원과 큰 삼각형은 얇은 검정/회색 손그림으로 둔다.
  - 학생 시선 이동 순서: 지름 `BC` → 원주각 직각 → `CD=3` → 음영 넓이 → `CE=k` → 정답 `48`.

## 2. 좌표계/구성 데이터

- 좌표계 정의: 원본 형태 보존용 내부 좌표 `B=(0,0)`, `C=(5,0)`, `A=(2,4)`, 반원 중심 `O=(2.5,0)`, 반지름 `2.5`.
- 축 범위/스케일: 좌표축은 출력하지 않는다. 도형은 원본처럼 `B` 왼쪽 아래, `C` 오른쪽 아래, `A` 위쪽에 배치한다.
- 필수 점: `A=(2,4)`, `B=(0,0)`, `C=(5,0)`, `D=(16/5,12/5)`, `E`는 원 `distance(E,O)=2.5`와 작은 호 `CD` 위 조건 및 `CE<DE`를 만족하는 점.
- 필수 선/곡선/호: 선분 `AB`, `BC`, `AC`, 위쪽 반원 `BC`, 선분 `DE`, `EC`, `DC`.
- 필수 음영/색칠 경계: 삼각형 `DCE` 내부만 옅은 파란색 또는 손칠형 빗금. 반원 부채꼴, 사다리꼴, 현 아래 전체 영역처럼 보이면 실패.
- 필수 라벨: `A`, `B`, `C`, `D`, `E`, `BC=5`, `CD=3`, `CE=k`, 필요 시 `90°` 직각 표시.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - 분수는 가능한 세로분수 형태로 표기한다.
  - 함수값/개수 라벨은 `h(1)`처럼 교육과정 표기로 쓴다.
  - 수식/분수/그래프 라벨은 `scripts.harness.labels`의 kyunghoonmath 렌더링 유틸을 사용한다.
  - Q10 도형은 점/길이 라벨 중심이며, 라벨은 본문 글씨와 비슷한 손글씨 크기로 둔다.
- 금지 라벨/표현:
  - `pi`, `h1`, `h2`, `18/5` 단순 가로식, 진한 불투명 음영
  - `E`가 현 `CD` 위 점처럼 보이는 배치
  - `E`가 반원 밖/안쪽 임의점처럼 보이는 배치
  - `A-D-C`가 꺾인 선처럼 보이는 배치
  - 좌표축 도식이 원문 도형을 대체하는 표현

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: 문제 원문 아래와 풀이 시작부 사이 또는 풀이 초반 오른쪽 흐름에 조화롭게 배치한다. 독립 흰 박스 테두리처럼 보이면 실패.
- 최대 박스 높이: 전체 페이지 높이의 28% 이하. 단, 라벨 가독성을 위해 충분한 폭을 확보한다.
- 비그래프 손글씨 영역 침범 금지 기준: 문제 본문, `호빈T의 팁`, `풀이)` 시작부와 최소 30px 이상 떨어지고, 라벨이 풀이 글씨를 덮지 않는다.
- 라벨 잘림 방지 기준: 모든 점/길이 라벨은 도형 박스 안쪽 20px 이상 여유를 둔다.
- 그래프가 설명을 돕는 방식: 원주각 90도, `CD=3`, `CE=k`, 음영 `△DCE`가 한눈에 보여야 하며, 풀이 계산과 연결되어야 한다.

## 4. 하네스 assert

- [ ] 모든 필수 점이 박스 안에 있다.
- [ ] 모든 필수 라벨이 잘리지 않는다.
- [ ] 원/호/곡선 위 점은 거리/방정식 검증을 통과한다.
- [ ] `abs(distance(D,O)-2.5) < tolerance`.
- [ ] `abs(distance(E,O)-2.5) < tolerance`.
- [ ] `D`는 `AC`와 반원 위에 있다.
- [ ] `E`는 작은 호 `CD` 위에 있고 `CE<DE`가 시각적으로도 읽힌다.
- [ ] 음영은 지정된 영역만 칠한다.
- [ ] 축/곡선/기준선이 음영에 가려지지 않는다.
- [ ] 금지 라벨 문자열이 없다.
- [ ] 수식/분수 라벨이 kyunghoonmath 유틸로 렌더링되었다.
- [ ] 원 함수 그래프가 도형/음영 때문에 잘려 보이지 않는다.
- [ ] 교재형 해설 순서가 보인다: 원 함수 → 기준점 → 보조선/도형 → 음영/결론.
- [ ] 그래프/도형이 해설 본문을 덮지 않는다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: 원본 도형 형태를 보존하고, 좌표 검산으로 `D`, `E` 위치를 고정한다.
- 수학 불변량 검증: `AB=2√5`, `AC=BC=5`, `CD=3`, `CE²=4/5`, `60k²=48`.
- 교육과정 표기 검증: 점/선분/길이 라벨만 사용하고 코드식 라벨을 금지한다.
- 설명 도구로서의 적합성: `E`가 호 위에 있고 음영 `DCE`가 넓이 조건과 `CE=k` 계산을 설명해야 한다.
- 최종 판정: visual gate와 blind review 통과 전 등록 금지.
