# Visual Spec: 마전고 Q01-Q02 작업05 전체 재생성

```yaml
schemaVersion: handsolution-visual-spec-v1
runId: majeongo-q10-q01q02-stabilized-regeneration-202606201134
problemCode: majeongo-g2-june-q01-q02
workId: 작업05
sourceType: coordinate-rendered
visualKind: inequality-graph
status: spec_ready
```

## 1. 원본/목적

- 원본 그래프/도형 존재 여부: no
- 원본 참고 이미지 경로: `none`
- 그림 목적: 풀이 보조
- 학생이 이 그림에서 읽어야 할 관계:
  1. Q02 부등식 `2cos x-√2≤0`은 `cos x≤√2/2`로 바뀌고, 해는 x축 범위로 읽는다.
  2. 경계는 `π/4`, `7π/4`이며, 해 구간은 `π/4≤x≤7π/4`이다.
  3. Q01과 Q02의 풀이와 `호빈T의 팁`은 한 페이지 안에서 서로 구분되어야 한다.
- 교재형 시각 설계:
  - 원 함수 그래프 중 반드시 보존할 부분: 원문 그래프는 없으므로 보존 대상은 없다. 보조 그래프는 `y=cos x`와 기준선 `y=√2/2`를 얇고 명확하게 보여준다.
  - 도형/보조선을 얹는 순서: 축 → `y=cos x` 곡선 → 기준선 `y=√2/2` → 경계점 `π/4`, `7π/4` → x축 해 구간 실선/형광띠.
  - 강조할 부분과 약하게 둘 부분: 해 구간 실선/띠와 경계점은 강조하고, 곡선/기준선은 풀이 보조 수준으로 얇게 둔다.
  - 학생 시선 이동 순서: 부등식 변형 → 기준선 교점 → x축 해 구간 → `β-α=3π/2`.

## 2. 좌표계/구성 데이터

- 좌표계 정의: Q02 보조 그래프. x축 범위 `0≤x<2π`, y축 범위는 `-1.2≤y≤1.2` 정도로 둔다.
- 축 범위/스케일: `0`, `π/4`, `π`, `7π/4`, `2π`가 들어가며, `π` 표기는 수식 파이를 쓴다.
- 필수 점: `x=π/4`, `x=7π/4`에서 `y=√2/2`와 `y=cos x`의 교점. 해 구간 시작/끝점.
- 필수 선/곡선/호: `y=cos x` 곡선, 기준선 `y=√2/2`, x축 해집합 표시선 `π/4≤x≤7π/4`.
- 필수 음영/색칠 경계: 진한 면 채움 금지. 해가 되는 x범위만 x축 위 실선/얇은 형광띠로 표시한다. 곡선 아래 전체 면을 칠하지 않는다.
- 필수 라벨: `y=cos x`, `y=√2/2`, `π/4`, `7π/4`, `π`, 필요 시 `해` 또는 `α`, `β`.
- 라벨 표기 규칙:
  - `π`는 수식 파이로 표기한다.
  - 분수는 가능한 세로분수 형태로 표기한다.
  - 함수값/개수 라벨은 `h(1)`처럼 교육과정 표기로 쓴다.
  - 수식/분수/그래프 라벨은 `scripts.harness.labels`의 kyunghoonmath 렌더링 유틸을 사용한다.
  - `√2/2`, `π/4`, `7π/4`는 세로분수 또는 교육과정에서 읽기 쉬운 분수 라벨로 둔다.
- 금지 라벨/표현:
  - `pi`, `h1`, `h2`, `18/5` 단순 가로식, 진한 불투명 음영
  - `y=1/2`처럼 Q02 기준값을 틀리게 쓰는 표현
  - 그래프를 가리는 진한 보라색 면 채움
  - 어느 문제의 팁인지 불분명한 `Tip` 박스
  - Q01/Q02 문제 원문이 색상 박스 안에 과하게 들어가 원문이 풀이처럼 보이는 구성

## 3. 박스/배치 안전 규칙

- 그래프/도형 박스 위치: Q02 풀이 근처의 작은 보조 그래프. Q01 풀이와 Q01 Tip 영역을 침범하지 않는다.
- 최대 박스 높이: 기본형 묶음 페이지에서는 전체 높이의 18% 이하. 그래프보다 원문/풀이/정답의 가독성을 우선한다.
- 비그래프 손글씨 영역 침범 금지 기준: Q01/Q02 각각의 문제, 풀이, Tip, 정답이 분리되어 읽혀야 하며 그래프 라벨이 풀이 글씨와 겹치지 않는다.
- 라벨 잘림 방지 기준: x축 범위와 경계점 라벨은 그래프 박스 안쪽 16px 이상 여유를 둔다.
- 그래프가 설명을 돕는 방식: 부등식 해집합이 x축 범위로 읽히게 하고, 풀이를 대체하지 않는다.

## 4. 하네스 assert

- [ ] 모든 필수 점이 박스 안에 있다.
- [ ] 모든 필수 라벨이 잘리지 않는다.
- [ ] `π/4`, `7π/4` 경계점이 기준선 `y=√2/2`와 `y=cos x` 교점에 있다.
- [ ] 해집합 x범위 `π/4≤x≤7π/4`가 실선/띠로 보인다.
- [ ] 진한 불투명 음영이 없다.
- [ ] 축/곡선/기준선이 음영에 가려지지 않는다.
- [ ] 금지 라벨 문자열이 없다.
- [ ] 수식/분수 라벨이 kyunghoonmath 유틸로 렌더링되었다.
- [ ] 원 함수 그래프가 도형/음영 때문에 잘려 보이지 않는다.
- [ ] 교재형 해설 순서가 보인다: 원 함수 → 기준점 → 보조선/도형 → 음영/결론.
- [ ] 그래프/도형이 해설 본문을 덮지 않는다.
- [ ] `호빈T의 팁`이 Q01/Q02 중 어느 문제의 것인지 명확하다.

## 5. Review Agent 체크

- 원본참고/좌표렌더링 방침 준수: 원본 그래프 없음. Q02 풀이 보조 그래프는 좌표/함수식으로 렌더링한다.
- 수학 불변량 검증: Q01 정답 `⑤ 4π/3`, Q02 해 구간 `π/4≤x≤7π/4`, `β-α=3π/2`, 정답 `④`.
- 교육과정 표기 검증: `π`, `√2/2`, 분수 라벨, 구간 표기를 고등학교 표기로 쓴다.
- 설명 도구로서의 적합성: 그래프는 부등식 해석을 돕는 장치이며, Q01/Q02 풀이와 Tip을 흐리지 않는다.
- 최종 판정: visual gate와 blind review 통과 전 등록 금지.
